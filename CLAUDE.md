# PathFinder — Claude Code Project Memory

This file is auto-loaded by Claude Code at the start of every session. Keep it lean — full decision history and rationale lives in `master-spec-doc.md`, not here. Read that file for deeper context; treat this one as the fast-load summary.

## What this is
A free, student-owned app guiding immigrant and first-generation students (and their families) through college prep, grades 6–12. Core differentiator: real, honest, specific content — not generic AI-generated advice. See `master-spec-doc.md` Section 1 for the full mission.

## Tech stack
- Frontend: Next.js (App Router) + Tailwind v4 + TypeScript, at `app/` — the real production app, not a prototype. `pathfinder-app.jsx` at the repo root is historical reference only.
- Backend/DB: Supabase project — URL `https://kvnmydvsffjvrsndnawd.supabase.co` — DO NOT create a new project, this one has real signups. Real email/password + Google OAuth, session refresh via `src/proxy.ts` (Next.js 16 renamed Middleware→Proxy), route gating, and a `profiles`/`roadmap_progress`/`activities`/`chat_messages`/`share_links`/`courses` schema (see `supabase/migrations/`). **Migrations 0001–0009 confirmed applied**; **0010 (saved opportunities) and 0011 (goals_notes) are written but NOT yet run** — both degrade gracefully until they are (the save controls hide themselves; the goals field is simply absent from AI context).
- Hosting: **Vercel — live at https://pathfinder-atharv.vercel.app, auto-deploying on every push to `master`.** Project name `pathfinder`, connected to the GitHub repo via the Vercel GitHub App. Root Directory is explicitly set to `app` (don't let it revert to auto-detect — breaks path aliases on git-triggered builds). SSO/Deployment Protection is off (public app). See `master-spec-doc.md` Decisions Log, Aug 11 entry, for the full setup story if this ever needs revisiting.
- Repo: `atharv146/pathfinder` (public) — https://github.com/atharv146/pathfinder
- Vercel Analytics + Speed Insights are live (cookieless, no key, no personal data). Deliberately not product analytics — per Section 7, we measure whether pages load, never what a student entered or asked.

## ⚙️ How to ship — use this, don't hand-roll it

```bash
./scripts/ship.sh "what you changed"   # verify → commit → push
./scripts/ship.sh                      # verify → push existing commits
```

Runs `typecheck → lint → test → build` and **refuses to push if any of it fails**. Every push to `master` auto-deploys to production with no staging step and no review gate, so this being the default path is the point.

- `cd app && npm run verify` runs the same checks without pushing.
- `npm test` (vitest) — **58 tests**, pure logic and data integrity only, no DOM/render tests. Covers course matching, the opportunities/scholarships merge, AI spend caps, guide-content structure, and the OpenRouter model-chain shape. Two real bugs that shipped on Aug 17 have regression tests (a false course match, a duplicate-key bug). The freshness tests will start failing on their own once scholarship/opportunity data passes a year old — that is deliberate, and the fix is to re-verify the data, never to bump the date.
- **CI is live on `master`** (`.github/workflows/verify.yml`, pushed via GitHub's web editor to sidestep a token-scope block — see the note in the Aug 17 Decisions Log if this ever needs redoing). Runs the same four checks on every push/PR as a second line of defence behind `ship.sh`.
- Pushing from this machine works — `credential.helper = osxkeychain` holds a working token. The one real limit: it lacks the `workflow` scope, so a *local* commit touching `.github/workflows/**` still gets rejected in full by `git push`. `ship.sh` detects this and explains rather than failing cryptically; the fix each time is the web-editor route above, not fighting the token.

## 🔑 Session handoff (Aug 17, 2026 — read this first)

**Everything queued as of the last handoff is now built.** This was a long single day of sessions — parent-account onboarding, the `/stats` page + course schema, the scholarships→opportunities merge, a text-rendering bug fix + perf pass, full test/CI/automation setup, the guide-articles depth pass, scholarship/opportunity research completion, a documented mobile audit, an AI-spend hole closed, lint debt reduction, and a real bug fix in the OpenRouter model list. Full narrative detail for each is in `master-spec-doc.md` Sections 16P–16U — this box is the condensed, current-state version.

**Design is signed off as final (Aug 17, 2026, user's explicit words: "everything you have done so far is good... im signing off on design after this").** Stop treating the design system as provisional — the accent/backdrop/typography system, the maximalist direction, all of it, is the shipped look. Don't re-propose or re-litigate it without the user raising it first.

**What a new session should actually pick up (nothing else is queued):**
1. **Two migrations need running in the Supabase dashboard** — `0010_saved_opportunities.sql` (star/save on `/opportunities`, plus a Saved filter) and `0011_goals_notes.sql` (the free-text "what do you actually want" box on `/stats`, which feeds Ask AI and Profile Analysis). Both are written, shipped, and inert until run; nothing else breaks meanwhile.
2. **Content expansion is a standing, ongoing background task now** (the user's explicit instruction: "keep expanding any piece of content that can be perfected... keep adding content"). `/opportunities` is now **46 entries** (16 scholarships + 30 major-linked), and the parent guide has **8 articles** — a 7th, "Community College and Transfer Pathways," and an 8th, "After the Decisions Arrive" (waitlists, deferrals, comparing offers apples-to-apples, the May 1 deposit and why double-depositing gets offers rescinded, and conditional-admission risk). **Every major-opportunity family now has 3+ entries** — `education` was the last thin one, closed with the AAPT Barbara Lotze Scholarship for Future Physics Teachers. This is genuinely never "done" — treat it as ongoing unless the user says to stop.
2. **`OPENROUTER_API_KEY` is now set, both locally (`app/.env.local`) and in the Vercel dashboard** (user-confirmed Aug 17, 2026) — the resume-rewrite call in Profile Analysis is live on real OpenRouter free-tier rotation now, not just the Gemini fallback. If a future session sees Gemini-only behavior on that route, that's a regression to investigate, not the expected state anymore.
3. **Provider upgrade to a paid model (Claude Sonnet), if usage ever justifies real spend:** `ANTHROPIC_API_KEY` is reserved and unused — see the PROVIDER note in `src/app/api/chat/route.ts` before wiring it in. Still deliberately deferred per the user's explicit instruction earlier this same day.
4. **The lint debt note** (26 React Compiler warnings, all inside verified WebGL/animation code, downgraded to warnings with the reasoning in `eslint.config.mjs`) is recorded debt, not urgent — see that file before touching it.
5. **The GitHub PAT still lacks the `workflow` scope** — not blocking (CI is live via the web-editor route), a nice-to-have. Fix: generate a new classic PAT at github.com/settings/tokens with `repo` + `workflow` scopes, then locally run `printf "protocol=https\nhost=github.com\n" | git credential-osxkeychain erase` and let the next `git push` prompt for the new token.

**Nothing else is blocked or pending.** If a future session finds itself unsure whether something is built, `grep` the codebase or check `master-spec-doc.md` Section 15 before assuming — this file and that one are both kept current as of this handoff.

### The highlights a future session must not undo

- **`.split-line-mask` in globals.css is load-bearing.** GSAP `mask: "lines"` clips at the padding box and `.display` uses `line-height: 0.95`, so descenders were being sliced off every animated heading (measured: 12.5px of ink at a 100px font, real user report: "text cut off at the bottom of the words"). **Any component using `mask: "lines"` MUST pass `linesClass: "split-line"`** or no CSS can reach its mask.
- **Never put a `scale()` in the `.aurora` keyframes, and don't raise the blur back to 90px.** That combination re-rasterises a 2.5×-viewport blurred layer every frame on every page and was the biggest single cause of the site feeling laggy — bigger than all the WebGL combined.
- **Canvases pause when off-screen** via `lib/useCanvasActive.ts` → R3F `frameloop`, and share one capability gate via `lib/useWebglGate.ts` (`useSyncExternalStore`, not an effect). Any new `<Canvas>` should use both.
- **`/tools/profile-analysis` is the flagship** (`rose` accent, `lattice` backdrop, promoted on the homepage). It never scores a student, never prints a college statistic we haven't verified — the dream-college section teaches the Common Data Set and net price calculator instead. Its only AI call rewrites the student's own activity entries under the never-inflate rule in `lib/ai/resume-prompt.ts`.
- **`lib/analysis/structure.ts` matches course names by digit-first rules on purpose** — "Algebra I" once matched the "Algebra 2" step, which would have told a student they'd completed a class they hadn't. Regression-tested.
- **`/opportunities` (not `/scholarships`, not "Money") is the real directory now** — 16 scholarships + 30 major-linked programs/internships/competitions, merged from `data/scholarships.ts` and `data/major-opportunities.ts` via `lib/opportunities.ts` without rewriting either source file. `/scholarships` redirects there. **Don't rename the nav back to "Money" or re-split it** — that was a direct user correction. Golden Door and Ron Brown are both listed now (Golden Door with its eligibility gap stated on the card, since the org's own page still doesn't publish one). **The homepage promo and `/opportunities`' own hero copy were both reframed Aug 17, 2026** from "Money, and everywhere else it comes from" to "Real experience, not just free money" — a direct user correction ("dont advertise it as money advertise as ec... from job or internshps"). Don't revert to money-first framing.
- **Guide articles got the systematic depth pass** (+45% content, every added figure verified against a primary source — Pell/loan limits, FERPA, CSS Profile). They are no longer "good but shallow"; treat them as done unless a specific fact needs re-checking.
- **AI spend has two dashboard levers with no deploy needed:** `AI_DAILY_CAP` (per-user cap across chat+interview+analysis, defaults to 30) and `AI_DISABLED=1` (hard kill switch, honest message, rest of the app keeps working). Both tested in `lib/ai/guard.test.ts`.
- **`OPENROUTER_API_KEY` is documented and wired, but genuinely optional.** Powers only the resume-rewrite call inside Profile Analysis (never chat/interview — those sit behind the immigration-enforcement guardrail, where a free unvetted model tier is too much risk). The free-model list in `lib/ai/openrouter.ts` was verified against OpenRouter's live `/api/v1/models` endpoint directly (not a summarized fetch, which truncates a 400+ model response) — **re-verify against that live endpoint before trusting the list again after any real time gap**, free-tier availability churns fast. Unset key = falls back to the Gemini chain, a normal working state.
- **Touch targets got a real fix, not a redesign.** `.tap-target` in globals.css expands the *hit area* via an `::after` pseudo-element without touching layout — used on "Mark as done" (was 17px) and several back-links. Don't apply it inside tight vertical lists; it can steal a neighbour's tap.
- **`--color-smoke` was raised** (#78777f → #8b8a92) after measuring it under WCAG AA on panel backgrounds, not eyeballing it. Standing rule: always measure contrast, never eyeball.

## Current status (update this section, keep it current — don't let it go stale)

- **The real app exists and runs, fully deployed.** `npm run dev` (via `.claude/launch.json`) and `npm run build` both work clean. Live routes: `/`, `/roadmap`, `/roadmap/[grade]`, `/guide`, `/guide/[slug]` (9 articles), `/major`, `/opportunities` (redirects: `/scholarships`), `/tools`, `/tools/fee-waivers`, `/tools/essay-brainstorm`, `/tools/profile-analysis`, `/stats`, `/account`, `/activities`, `/ask-ai`, plus `/login`, `/signup`, `/onboarding`. Repo and `origin/master` are in sync.
- **Accounts/auth:** email/password + Google OAuth (`src/components/auth/AuthForm.tsx`), real session refresh (`src/proxy.ts`), route gating (public: `/`, `/login`, `/signup`, `/guide`+, `/s/[token]`; gated: everything else). DB schema in `supabase/migrations/` — all RLS-enabled, owner-only policies. Roadmap "mark as done" is DB-backed. Account deletion (`/api/account/delete`) is real and correctly scoped.
- **Onboarding now branches by role** (added Aug 17, 2026 — see `src/components/onboarding/OnboardingFlow.tsx`). Parents get the standalone-account promise on screen the moment they pick "parent," are never asked to identify their child, answer one fewer question, and land on `/guide` instead of a roadmap. **Do not add a "link to my child's account" feature** — standalone is the July 21, 2026 decision, and onboarding now states that to the user's face. Students still land on their grade's roadmap as before. Still light by design (grade + role + major, all skippable) — the fuller profile (GPA, test scores, target colleges, status, course list) lives on `/stats`, reachable but not gated behind signup.
- **`/account` vs `/stats` split (Aug 17, 2026):** `/account` is identity/settings only — email, language/role, share link, deletion. `/stats` holds everything that shapes guidance — grade, major, the per-course list (`src/lib/db/courses.ts`, migration 0008), and four student-reported school-context fields (AP count band, IB, dual enrollment, free-text course limits). **Don't re-add profile fields to `/account`** — the relocation was deliberate. The course list and school context never score a schedule; they exist because two APs at a school offering three reads completely differently from two at a school offering twenty-five, and `buildContextBlock()` feeds both to the AI with an explicit describe-don't-rank instruction.
- **Content:** roadmap — `content/roadmap-content-v4.md` → `app/src/data/roadmap.json`, 51 items. Parent guide — `app/src/data/guide-articles.json`, **9 articles** (added Aug 17, 2026: "Community College and Transfer Pathways", "After the Decisions Arrive", "How to Actually Do Research in High School"), genuinely deepened across the original six (+45% content, every added fact verified against a primary source: ED's Pell/loan figures, FERPA, CSS Profile, Common App mechanics, residency classification). Freshness stamps (`data/freshness.ts`) on every article name exactly what was checked and when. `guide-articles.test.ts` asserts the article count directly — bump it if you add another.
- **Major-family guidance:** `src/data/majors.ts` (8 buckets, structurally-safe notes) + `src/data/major-pathways.ts` (course ladders, 4-stage grade breakdown, structure facts per family) power `/major` — accent `azure`, backdrop `orbits`. `MajorLens` on the roadmap is a deliberate one-line pointer, not a re-grown card. **All 8 major-opportunity families are fully researched** (`src/data/major-opportunities.ts`) as of Aug 17, 2026 — `social-sciences` was the last, closed out with 4 newly-verified entries.
- **`/opportunities` (V2 §16K, merged Aug 17, 2026, promoted on the homepage later the same day):** unified directory — `lib/opportunities.ts` merges `data/scholarships.ts` (**16 entries**) with `data/major-opportunities.ts` (**30 entries**, each tagged `kind: "scholarship" | "program" | "internship" | "competition"`) into one searchable, filterable list — **46 entries total** as of the latest content-expansion pass. **`DeadlineTimeline`** (`src/components/opportunities/DeadlineTimeline.tsx`) sits above the directory: a twelve-month track plotting only scholarships with a published `closesOn`, sharing `DeadlineOrbit`'s exact urgency thresholds (14d red / 60d amber / else calm) so both surfaces read as one system. It deliberately omits undated and already-closed awards and says how many it omitted, rather than inventing positions. Neither source file's own verification discipline was touched. `/scholarships` redirects, doesn't delete. Nav label is `"Opportunities"` — do not revert to `"Money"`. **`OpportunitiesPromo` on the homepage** (`src/components/home/OpportunitiesPromo.tsx`, right after `AnalysisPromo`) advertises it with a live entry count pulled from `allOpportunities()` rather than a hardcoded number, `lime` accent (the page's own color, same reasoning as `AnalysisPromo` wearing `rose`). Includes Youth Conservation Corps — the first entry on the whole list that's a genuine paid job rather than a scholarship/stipend/competition — plus HOSA, National History Day, FBLA, Diamond Challenge, National Merit and Burger King Scholars from the newest content pass.
- **`/tools/profile-analysis` — the flagship (built Aug 17, 2026):** `rose` accent, `lattice` backdrop, promoted on the homepage via `AnalysisPromo`. Three parts: (1) deterministic course-vs-major-ladder matching (`lib/analysis/structure.ts`, digit-aware so "Algebra I" can't match "Algebra 2"), (2) AI activity rewriting under a strict never-inflate rule (`lib/ai/resume-prompt.ts`, provider chain OpenRouter free-tier → Gemini fallback), (3) a college-list section that teaches the Common Data Set and net price calculator instead of printing any unverified statistic. No score, ever, anywhere on the page.
- **AI backend — three surfaces, one shared guard (`lib/ai/guard.ts`):** Ask AI, the activities interview, and Profile Analysis's resume rewrite. All AI spend (chat + interview + analysis) counts against one shared daily cap, now correctly enforced everywhere after the Aug 17 fix (Profile Analysis previously checked the cap but never wrote a row — effectively uncapped). `AI_DAILY_CAP` and `AI_DISABLED=1` are dashboard-settable, tested. Gemini model chain: `gemini-3.7-flash` → `gemini-3.5-flash` → `gemini-3.1-flash-lite`, `MODEL_CHAIN` in `guard.ts`. `lib/ai/openrouter.ts` is a second, provider-level fallback scoped only to the resume-rewrite call, with a verified-live free-model list.
- **Safety rule, load-bearing, re-test after any prompt/model change:** `system-prompt.ts` never reassures anyone about immigration-enforcement risk and never states state-level aid policy as settled fact — found by actually testing the model (Gemini once volunteered false FERPA-based reassurance), not by reasoning about it in the abstract.
- **Gap analysis:** `WhereYouAre` on `/roadmap` never scores the student — shows what's still open, walked newest-grade-first, with evergreen-vs-windowed item filtering so a late joiner doesn't see five years of unchecked boxes.
- **Tools (`/tools`, sign-in required):** `FeeWaiverChecker` never says "you don't qualify" (thresholds move annually — false negatives cost real money). `EssayBrainstorm` writes nothing, ever (an AI-drafted essay is an integrity problem and would blacklist the app with counselors).
- **Parent mode + Spanish:** `account_type` finally has a UI (now set at signup, see Onboarding above, plus a settings toggle at `/account`). Spanish covers UI chrome only (`lib/i18n/strings.ts`) — roadmap/guide content is deliberately NOT machine-translated; `ExplainInSpanish` hands the reader to the AI instead, labelled as an explanation not an official translation.
- **Counselor share link:** student-initiated and student-revocable only, never parent-initiated. `get_shared_progress` (SECURITY DEFINER) is the only place data leaves an account — its field list is a privacy boundary, not a feature list.
- **Design:** true-black grounds, Figtree display face, per-page accent (`teal` home · `lime` roadmap/opportunities · `coral` guide · `violet` ask-ai/account/stats · `azure` major · `rose` profile-analysis) + 3D backdrop system (`grid`/`sheets`/`swarm`/`orbits`/`strata`/`lattice`, one geometry per page, never a recolour). **Signed off as final by the user Aug 17, 2026** — treat the system as settled, not provisional.
- **Performance (Aug 17, 2026 pass):** the site's biggest lag source was a CSS blur (`.aurora`), not WebGL — a `scale()` in its keyframes forced a full re-rasterise of a 2.5×-viewport blurred layer every frame. Fixed (translate-only, smaller blur/inset), plus canvases now pause off-screen/hidden-tab, composer MSAA off, DPR caps lowered, grain layer shrunk. See the handoff box above for the do-not-undo list.
- **Text rendering (Aug 17, 2026 fix):** GSAP `SplitText`'s `mask: "lines"` was slicing descenders off every animated heading — see `.split-line-mask` in the handoff box above.
- **Mobile:** a real, documented audit (17 routes at 375px, Aug 17, 2026) found zero overflow and zero iOS-zoom triggers; the one real finding (touch targets) was fixed with a hit-area utility, not a restyle. This supersedes the earlier "three separate symptom-driven commits, not a full audit" state.
- **Tests, CI, automation (Aug 17, 2026):** 58 vitest tests (pure logic/data only), `scripts/ship.sh` as the one-command ship path, CI live on `master`. See "How to ship" above.
- **Platform note:** development happens on a MacBook. No project migration needed — clone fresh, `npm install` in `app/`. Nothing machine-specific is checked in.
- **The ML chancing-model question is RESOLVED (Aug 16, 2026) — don't re-open it.** The trained model stays the brother's side project and never ships into PathFinder; Profile Analysis's dream-college comparison uses NCES/Common Data Set aggregate ranges instead. Full build plan for the side project itself (verified dataset situation, methodology, honest framing) is in `master-spec-doc.md` §16O — read it before saying anything about that project.
- **Build order** (see `master-spec-doc.md` Section 16): steps 1–7 of the original sequence are all done or substantially done; step 6 (accounts/backend) landed well ahead of finishing steps 3–5, so the sequence is non-linear — don't assume anything is untouched without checking this file first.

## 🚨 READ FIRST — motion levels, and the bug that wasted several sessions
**The user's machine has macOS "Reduce Motion" enabled.** Every animated surface used to gate on `prefers-reduced-motion` directly, so the 3D hero, physics pit, all page backdrops, the wire cage and every scroll reveal were correctly and silently switched **off** for them. They spent multiple rounds reporting "I don't see any 3D, nothing moves, it's static" — and they were right. Measured proof: same page, `canvasCount` went 0 → 4 after the fix.

**Never gate animation on `matchMedia("(prefers-reduced-motion: reduce)")` directly again.** Use `src/lib/motion.ts`:
- `shouldRender3D()` — true at `full` *and* `calm`; only `still` removes geometry.
- `shouldAnimateAggressively()` — `full` only. Gate bounce/overshoot, scroll-scrub, parallax and Lenis behind this.
- OS reduced-motion maps to **`calm`, never `still`** — reduce motion means calmer motion, not a dead page.
- An explicit user choice (localStorage `pf-motion`) always beats the OS. `MotionToggle` renders the control plus a first-run notice explaining the situation when the OS is suppressing things.
- **`src/lib/useWebglGate.ts`** (added Aug 17, 2026) is the single `useSyncExternalStore` hook every WebGL surface should use for the motion-gate + capability-probe combo — four components used to carry byte-identical copies of this as an effect. Don't hand-roll a fifth.

## ⚠️ Typography changed (Aug 14, 2026) — the serif is gone
**Display face is now Figtree (bold geometric sans), not Instrument Serif.** The serif was the single biggest reason the site read as a generic template beside the user's references (intrepidautomation.com, zypsy.com, rejouice.com) — all of which run a bold geometric sans. Use the `.display` / `.display-md` classes (weight 800/700, tight negative tracking), **not** `font-display` + `font-normal`. Instrument Serif is retained only as `--font-serif` for rare accents.

Also added and load-bearing:
- **Light-section inversion** (`bg-bone` `#eef0ed`, dark type inside). Intrepid alternates dark → light → dark, and this does more to break up a long page than any single animation. Use it; don't let the page become one black slab again.
- **`IntroLoader`** — colour-wash + counter + panel-wipe opening, once per tab via sessionStorage, with a hard 4.5s failsafe that always restores `body` overflow. It renders *over* a complete page, never as a gate in front of an empty one.
- **`WireCage`** — morphing hyperboloid built from straight lines (twist angle animates); `ClosingWire` sets huge type inside it.
- **`Marquee`** is full-bleed (`left-1/2 w-screen -translate-x-1/2`) with Zypsy-style boxed cards.

## ⚠️ Palette update (Aug 14, 2026) — colour is now allowed, as light
The strict near-monochrome rule below was **relaxed by the user**, who rejected the monochrome result repeatedly as "plain / boring / just black white and stars." Colour now enters as *light over true black*, never as flat pigment: `.aurora` (multi-stop drifting gradient field — **see the perf note in the handoff box above before touching this**), `.glow-signal` / `.glow-ember` (emissive type — **single words only, never paragraphs**), `.edge-glow` (gradient-masked panel borders), `.scanlines`. True black grounds, hairline geometry and the editorial structure all still stand. The one deliberate exception is `.paper` (cream) in `ResumePaper`, justified contextually because an application is physically a document — it is not licence for a cream site palette.

## Design direction (rewritten Aug 14, 2026 — the old restrictive version was retired at the user's explicit request)
The user's direction is **maximalist and premium**: heavy 3D, real physics, bold per-page colour, kinetic type. What replaced the earlier near-monochrome rules:

1. **Every page commits to ONE bold accent**, set via `data-accent` on `PageFrame` (`teal` home · `lime` roadmap · `coral` guide/tools · `violet` ask-ai/account/stats · `azure` major · `rose` profile-analysis). Use `.glow-accent`, `.aurora-accent`, `.text-accent` — never hardcode a hex.
2. **Each page gets its own 3D geometry, not the same scene recoloured** — `Backdrop variant="grid" | "sheets" | "swarm" | "orbits" | "strata" | "lattice"`. Add a new variant for a new page rather than reusing one.
3. **Text arrives, it doesn't fade** — `KineticText` (GSAP `back.out` overshoot) for headings; `SplitReveal` for calmer body-adjacent lines. **Both require `linesClass: "split-line"` on their `SplitText` call** — see the handoff box above.
4. Reference sites: zypsy.com, intrepidautomation.com, studio-size.com, edpnc.com, collectiveoffice.com, rejouice.com. Match these, not generic AI-landing-page conventions.
5. **Still true, and non-negotiable:** all WebGL is progressive enhancement behind a capability + reduced-motion gate with a real fallback; and **never author reveal-dependent content with inline `opacity: 0`** — hide at runtime, add a failsafe timeout, so failure leaves content visible rather than blank.
6. **⚠️ JSX WHITESPACE BUG.** Writing `{expr} word` where the following text **wraps onto another content line** renders as `valueword` — the compiler strips the leading space of a text run whose first line is followed by more content lines. **Fix: use an explicit `{" "}` after any expression followed by wrapping prose.**
7. **Radix (`@radix-ui/react-tabs`, `@radix-ui/react-tooltip`)** for headless interactive primitives — used on `/major` and `Tooltip.tsx`. **If you add a Tooltip trigger, give it `tabIndex={0}`** or Radix's `onFocus` reveal path has nothing to attach to.
8. **Local-dev auth bypass in `src/proxy.ts`.** Gated routes render without a session when `NODE_ENV === "development"` — safe, since that never happens on Vercel. Exists because Google sign-in on localhost bounces to the production Site URL (a dashboard config, not fixable in code).
9. **Never use `<AnimatePresence mode="wait">` for content swaps.** Exit animations run on rAF; wherever rAF is frozen (backgrounded tab, preview pane), the exit never completes and the page keeps showing the *previous* item's content under the *new* item's heading — confidently mislabelled content, the one thing this app must never do. Use a CSS keyframe (`.swap-in`, starts at `opacity: 0.55` not `0` — Chrome throttles hidden-document animations to their first frame) plus a React `key` instead.

## Content rules
- Never invent statistics, organization names, or policy specifics. If unsure, flag it for verification rather than guessing.
- All roadmap/guide/opportunity content must be genuinely researched and specific — generic filler is a regression, not a feature.
- Content stays general/major-agnostic for V1. Major-specific personalization is V2/V3 — see `master-spec-doc.md` Section 3B.

## Do NOT
- Add features outside V1/V2 scope without the user explicitly approving the scope change first.
- Regenerate or "improve" the roadmap/parent-guide/opportunity content without being asked — it's already been through multiple real research and revision passes.
- Make irreversible or costly decisions (recreating the Supabase project, force-pushing, deleting data) without explicit confirmation.
- Rename `/opportunities` back to "Money" or re-split it into a scholarships-only page.
- Add profile fields back to `/account` — they live on `/stats` now.
- Re-open the ML-chancing-model question — it's resolved, see above.

## After material changes
Update the Decisions Log and Current Build Status sections in `master-spec-doc.md`, and this file's "Current status" section — this is a standing user instruction, not optional.

## Reference files in this repo
- `master-spec-doc.md` — full mission, scope, decisions log, build status (the deep-context doc)
- `app/` — the real Next.js app (source of truth going forward)
- `content/roadmap-content-v4.md` — human-readable source of truth for roadmap content (parses into `app/src/data/roadmap.json`)
- `pathfinder-app.jsx` — historical prototype, superseded by `app/`. Still useful as a record of the original UI interaction patterns, but no longer the source of truth for content or code.
