# PathFinder — Claude Code Project Memory

This file is auto-loaded by Claude Code at the start of every session. Keep it lean — full decision history and rationale lives in `master-spec-doc.md`, not here. Read that file for deeper context; treat this one as the fast-load summary.

## What this is
A free, student-owned app guiding immigrant and first-generation students (and their families) through college prep, grades 6–12. Core differentiator: real, honest, specific content — not generic AI-generated advice. See `master-spec-doc.md` Section 1 for the full mission.

## Tech stack
- Frontend: Next.js (App Router) + Tailwind v4 + TypeScript, at `app/` — the real production app, not a prototype. `pathfinder-app.jsx` at the repo root is historical reference only.
- Backend/DB: Supabase project — URL `https://kvnmydvsffjvrsndnawd.supabase.co` — DO NOT create a new project, this one has real signups. **Wired into the Next.js app as of Aug 15, 2026** — real email/password + Google OAuth, session refresh via `src/proxy.ts` (Next.js 16 renamed Middleware→Proxy), route gating, and a `profiles`/`roadmap_progress`/`activities` schema (see `supabase/migrations/`). The anon + service_role keys were only added to `app/.env.local` on Aug 15, 2026 — before that the code existed but couldn't actually connect.
- Hosting: **Vercel — live at https://pathfinder-atharv.vercel.app, auto-deploying on every push to `master`.** Project name `pathfinder`, connected to the GitHub repo via the Vercel GitHub App. Root Directory is explicitly set to `app` (don't let it revert to auto-detect — breaks path aliases on git-triggered builds). SSO/Deployment Protection is off (public app). See `master-spec-doc.md` Decisions Log, Aug 11 entry, for the full setup story if this ever needs revisiting.
- Repo: `atharv146/pathfinder` (public) — https://github.com/atharv146/pathfinder

## 🔑 Session handoff (Aug 17, 2026, later session — read this first)

**Migration 0008 has been run by the user (confirmed Aug 17, 2026) — `/stats` is live.**

**§16K IS NOW COMPLETE — items 1–7 all built.** The later Aug 17 session shipped items 4–7 plus a perf pass and a real text-rendering bug fix. Full detail in `master-spec-doc.md` **Section 16Q**. Highlights a future session must not undo:
- **`.split-line-mask` in globals.css is load-bearing.** GSAP `mask: "lines"` clips at the padding box and `.display` uses `line-height: 0.95`, so descenders were being sliced off every animated heading (measured: 12.5px of ink at a 100px font). **Any component using `mask: "lines"` MUST pass `linesClass: "split-line"`** or no CSS can reach its mask.
- **Never put a `scale()` in the `.aurora` keyframes, and don't raise the blur back to 90px.** That combination re-rasterises a 2.5×-viewport blurred layer every frame on every page and was the main cause of the site feeling laggy — bigger than all the WebGL combined.
- **Canvases pause when off-screen** via `lib/useCanvasActive.ts` → R3F `frameloop`. Any new `<Canvas>` should use it.
- **`/tools/profile-analysis` is the flagship** (`rose` accent, `lattice` backdrop, promoted on the homepage). It never scores a student, never prints a college statistic we haven't verified, and its only AI call rewrites the student's own activity entries under the never-inflate rule in `lib/ai/resume-prompt.ts`.
- **`lib/analysis/structure.ts` matches course names by digit-first rules on purpose** — "Algebra I" once matched the "Algebra 2" step, which would have told a student they'd completed a class they hadn't.
- **Scholarships is a 12-entry filterable directory now.** Two researched awards (Golden Door, Ron Brown) are deliberately absent with reasons recorded at the foot of `data/scholarships.ts` — re-check those before adding anything else.

**§16K step 3 is BUILT (Aug 17, 2026) — `/stats` + the per-course schema. Full detail in `master-spec-doc.md` Section 16P.** New gated route `/stats` (violet, new `strata` backdrop), `courses` table + four student-reported school-context columns on `profiles`, `src/lib/db/courses.ts`, `src/components/stats/`. `/account` shrank to email + language/role + share link + deletion — **don't re-add profile fields there**, the relocation was the point. Grade and major moved to `/stats`, which also fixed `/major`'s "Change your major" link pointing at a page with no major control.

**The rules those components hold:** the course list never scores a schedule (counts only — no target, no denominator, no meter, same rule as `WhereYouAre`); `course_rigor` stays as the one-click alternative so the detailed list is never mandatory; the school-context block exists because two APs at a school offering three is not two at a school offering twenty-five, and every generic tool gets that wrong. `buildContextBlock()` already feeds all of it to the AI **with an instruction to describe rather than rank** — keep that instruction attached to the data if you touch it.

**Next up:** §16K item 4 (structural course-vs-major comparison logic, using `major-pathways.ts` + the new course data) or item 5 (OpenRouter + Claude Sonnet provider paths). Both unblocked, no decisions pending.

## Session handoff (Aug 17, 2026, earlier session)

**Everything through V2 steps 1–10 plus §16K steps 1–3 is built and pushed.** `origin/master` and local are in sync. The app has real auth, an AI activities interview that's the actual differentiator, a major-specific hub, a scholarships hub, restructured tools, freshness stamps, parent mode, Spanish UI, and a counselor share link.

**What's next, in order — full detail in `master-spec-doc.md` Section 16K and Section 15:**
1. ~~`/major` — dedicated page for major-specific content~~ — **BUILT Aug 16, 2026. See Section 16L.** New route + `src/data/major-pathways.ts` (course ladders, 4 grade stages, structure facts per family), an interactive grade timeline, a course-chain diagram, an 8-family comparison grid, `azure` accent + `orbits` backdrop. `MajorLens` on the roadmap is now a one-line pointer, and Opportunities moved to `/major`.
2. ~~Tools become individual pages instead of sections on `/tools`~~ — **BUILT Aug 16, 2026. See Section 16M.** `/tools` is an index over `src/data/tools.ts`; `/tools/fee-waivers` and `/tools/essay-brainstorm` are their own routes; `item-tools.ts` deep links now build from `toolHref()` (no hash links remain). Add `/tools/profile-analysis` to the `TOOLS` array when that ships.
3. ~~Scholarships / internships / programs hub~~ — **BUILT Aug 16, 2026. See Section 16N.** Gated `/scholarships`, `src/data/scholarships.ts`, five awards verified on official sites (Gates, Jack Kent Cooke, QuestBridge College Prep Scholars, Dell, TheDream.US). Open-right-now sorts to the top via `cycleStatus()`. **Adding entries requires real web research — the file header states the rules; do not improvise.**
4. ~~Settings/stats page + the per-course-list schema~~ — **BUILT Aug 17, 2026. See Section 16P.** `/stats`, migration 0008 (`courses` table + school-context columns), course data wired into the AI context block. **Migration not yet applied — see the action-required note at the top of this handoff.**
5. The flagship "Profile Analysis" tool — **fully UNBLOCKED, see below**
6. New OpenRouter (resume text) + Claude Sonnet (Profile Analysis) provider paths
7. Guide-articles depth pass — still the original unmodified Aug 11 port, the last big untouched content area

**RESOLVED Aug 16, 2026 — the ML chancing model stays a side project and does NOT ship into the product.** User decision, explicit and final. The dream-college comparison in Profile Analysis is therefore **fully unblocked**: build it on **published NCES / Common Data Set aggregate ranges** instead of any model output. Ranges are descriptive facts about a school's last admitted class, never a probability or a verdict about this student — see the framing rules in `master-spec-doc.md` §16N, especially the test-optional caveat, which most sites get wrong.

**⚠️ The brother's ML project now has a full, research-verified build plan — `master-spec-doc.md` Section 16O. Read it before saying anything about that project; don't improvise.** Short version: there is no clean public dataset of individual U.S. undergrad applicants with real admit/reject outcomes — confirmed by checking the College Scorecard API (institution-level only) and Opportunity Insights' Mobility Report Cards (college × income-bracket level even from a Harvard research team with IRS-linked data on 30M+ students). §16O turns that absence into the project's actual content: model on the real, honest Kaggle "Graduate Admission 2" dataset (grad-school/international — multiple models, SHAP feature importance, calibration curves), and write a sourced limitations section — citing the College Scorecard and Opportunity Insights findings above — explaining why it doesn't generalize to U.S. undergrad admissions and why no public dataset currently lets it. Title it honestly (not "chancing predictor"). It never feeds a PathFinder-facing number.

**Also fixed this session, not yet load-bearing knowledge above:** a real AI reliability bug (Gemini free tier 429/503 failures — now a 3-model fallback chain, see the Model choice bullet below) and a real scroll bug (Lenis's cached scroll height going stale when content grew after mount — fixed with a ResizeObserver). Both are in the bullets below; flagged here so they're not missed by only skimming this box.

## Current status (update this section, keep it current — don't let it go stale)
- **The real app exists and runs.** Scaffolded at `app/` — Next.js (App Router) + Tailwind v4 + TypeScript. `npm run dev` (via `.claude/launch.json`) and `npm run build` both work clean. Live pages: `/`, `/roadmap`, `/roadmap/[grade]`, `/guide`, `/guide/[slug]` (all 6 parent guide articles), `/ask-ai` (now a real backend — see the AI backend entry below), `/tools`, plus **`/login`, `/signup`, `/onboarding`, `/account`, `/activities`** (added Aug 15, 2026 — see Accounts/auth below) and **`/major`** (added Aug 16, 2026). Repo is connected and pushed — `atharv146/pathfinder`, local and `origin/master` in sync, though the design/motion work is uncommitted-vs-pushed in parts (check `git status` — no `gh` CLI/stored credentials on this machine as of the Aug 15 seventh design pass, so pushes need the user).
- **Accounts/auth (Next Steps step 6 — substantially built Aug 15, 2026, now live with real keys):** email/password + Google OAuth (`src/components/auth/AuthForm.tsx`), real session refresh (`src/proxy.ts`), route gating (public: `/`, `/login`, `/signup`, `/guide`+; gated: `/roadmap`, `/onboarding`, `/account`, `/ask-ai`). DB schema in `supabase/migrations/` — `profiles`, `roadmap_progress`, `activities`, all RLS-enabled, owner-only policies, auto-create-profile trigger. Roadmap "mark as done" is now DB-backed (`src/lib/db/progress.ts`), with a one-time migration that lifts each device's old localStorage progress into the DB rather than discarding it. Account deletion (`/api/account/delete`) is real and correctly scoped: session-verified user id only (not from request body), deletes `activities`+`roadmap_progress`+`profiles` rows *and* the actual `auth.users` record. **Known gap:** onboarding only collects grade + major-family (both skippable) — it does not collect target college, GPA, test scores, immigration status, first-gen flag, or home language from master-spec-doc.md Section 4's data model, and `account_type` always defaults to `student` (no UI path to register as a parent account yet, despite Section 14's "parent accounts are standalone" decision). Decide deliberately whether to expand onboarding or keep it deliberately light before building more on top of it.
- **Content:** roadmap source of truth is `content/roadmap-content-v4.md`, parses into `app/src/data/roadmap.json` (**51 items** as of Aug 15, 2026 — middle school grades 6–8 just got a real depth pass: `g6-math-placement`, `g7-hs-credit-courses`, `g7-fee-waiver-awareness`, `g8-world-language-sequence`, plus 2 rewrites). Parent guide content in `app/src/data/guide-articles.json` is still the original **unmodified** port from Aug 11 — the deeper pass (Next Steps Sequence step 3) has started for the roadmap but not yet for the guide articles. **Content audit Aug 15, 2026:** overall quality is genuinely specific and well-researched (named policies, dated changes, honest caveats), not generic filler — but two verified dating errors were found (FAFSA cycle references reading one full cycle stale, and the "no sibling discount" fact mis-dated to 2026-27 when it actually took effect 2024-25). See master-spec-doc.md Decisions Log for the full fix list.
- **Major-family guidance** (`src/data/majors.ts`, added Aug 15, 2026): 8 hardcoded major-family buckets with structurally-safe notes only (no admissions odds, no rankings, no per-major course sequences). Confirmed to stay inside the V1/V2 boundary from master-spec-doc.md Section 3B, not a V3 "multi-year pathway per major" build — the file's own header comment names that boundary explicitly.
- **`/major` page (V2 §16K step 1 — built Aug 16, 2026; full detail in `master-spec-doc.md` Section 16L):** major-specific content now has its own home instead of being a card on the grade roadmap. New content layer `src/data/major-pathways.ts` (course ladders + 4 grade stages of courses/activities/narrative-angle + structure facts, per family), components in `src/components/major/`, accent `azure`, backdrop variant `orbits`. **`MajorLens.tsx` is now deliberately a ONE-LINE pointer — don't re-grow it**; if something needs saying about a major it goes on `/major`. Opportunities moved there too, with the grade≥9 gate lifted on purpose (a student who navigated to their field's page is browsing deliberately; knowing programs exist years early is the advantage). **Rules the components exist to hold:** nothing on the page may read as a score (the timeline is not a progress bar, the ladder is not a checklist — same rule as `JourneyArc`/`WhereYouAre`); the compare grid's "locked in early" meter rates the *field's* sequence-dependence, never the student; course ladders are the common U.S. sequence with the caveat rendered *inside* the diagram, never stated as a requirement.
- **Mobile:** real fixes have landed (iOS input zoom, touch targets, a full mobile nav sheet that didn't exist before, WebGL/lag fixes, Google sign-in timeout) but as three separate symptom-driven commits, not the documented full responsive audit that Next Steps step 4 calls for.
- **Design (as of Aug 3, 2026 — read `master-spec-doc.md` Section 5 in full before touching styling):** went through two rejected iterations (amber/ember-on-near-black, then a bolder orange-gradient version) before the user supplied three reference videos, which were frame-extracted with ffmpeg and matched directly. Current system: true black (`#000`) grounds, Instrument Serif display type, hairline `OrbitField` orbital graphic, IBM Plex Mono corner labels, monochrome numbered stat index. This is the first pass not immediately rejected, but **not explicitly signed off as final** — confirm with the user before building further on top of it or treating it as settled.
- **Two real bugs were found and fixed in the design work, not just style opinions:** (1) a word-spacing bug in the scroll-reveal text component (whitespace-collapse inside an overflow-hidden wrapper — fixed with margin instead of a space character), (2) a genuine low-contrast bug (muted text measured 2.98:1 against the background, below WCAG AA's 4.5:1 minimum) — this was the real cause of the "text sinking into the screen" complaint, not animation or clipping. When tuning any muted/secondary text color, check computed contrast ratio, don't eyeball it.
- **Preview-tool caveat:** the sandboxed browser pane used for verification has `document.hidden === true`, which freezes `requestAnimationFrame` and can make working animations look broken during automated checks. `RevealText`/`FadeIn` carry a timed fallback that force-reveals content regardless, so this can't cause a real user-facing bug — but don't mistake the preview tool's own limitation for an app bug during verification.
- **Motion/3D layer (Aug 14, 2026 — uncommitted, needs a real-browser look):** GSAP (+ ScrollTrigger/SplitText/DrawSVG — all free now), Lenis smooth scroll synced to the GSAP ticker, and React Three Fiber are installed. Added `SmoothScroll`, `SplitReveal`, `CountUp`, `ScrollProgress`, `CustomCursor`, `Starfield`, `RoadmapPath` (scrubbed DrawSVG path, nodes sampled via `getPointAtLength`), `TruthSpotlight` (cursor-as-torch), `hero/HeroScene` + `hero/HeroVisual` (WebGL hero, **progressive enhancement only** — device-capability + reduced-motion gated, SVG `OrbitField` is the floor). All GSAP plugins register in `src/lib/gsap.ts` — import from there, not from `gsap` directly. **Rule learned twice now: never author reveal-dependent content with inline `opacity: 0`** — hide at runtime via JS and add a failsafe timeout, so a missed trigger leaves content visible instead of permanently blank. See `master-spec-doc.md` Aug 14 log entry.
- **Verification caveat got worse:** the preview pane now reports *both* `document.hidden === true` (rAF frozen — measured 0 frames/1.5s) *and* `prefers-reduced-motion: reduce`, so GSAP/framer/WebGL animation cannot be observed there at all, and post-scroll screenshots come back stale/black. Verify animated work with `npm run build` + programmatic DOM/geometry checks, and ask the user to eyeball the real browser. `?force3d=1` overrides the reduced-motion gate on the 3D hero for QA.
- **AI backend (V2 step 1 — built Aug 15, 2026):** `/api/chat` is a real server-side route (`app/src/app/api/chat/route.ts`) holding the provider key server-side — no key ever reaches the browser, which was the original prototype's bug. Prompt-caches the system prompt, and guards in order: signed-in → under a 30/day cap → bounded input. System prompt lives in `src/lib/ai/system-prompt.ts` (reviewable content, no dynamic values — interpolating any would break the cache; per-user context goes in the message turn). Escalation flagging in `src/lib/ai/flags.ts` — a keyword heuristic, explicitly NOT a classifier, that surfaces real human resources (988, counselor, licensed attorney) alongside the AI answer. Chat UI at `src/components/ask/ChatPanel.tsx`. **Originally built against `claude-opus-5`/`ANTHROPIC_API_KEY` — superseded two entries below by the Gemini switch. The live provider is Gemini; see that entry, not this line, for the current model.** Migration `supabase/migrations/0003_chat.sql` adds `chat_messages` + RLS + a SECURITY DEFINER rate-limit function.
- **Migrations 0003–0007 are ALL confirmed applied** as of this session (verified directly against the database, not assumed) — chat memory/cap, profile fields, `chat_messages.kind`, language preference, and share links are all live. If a future session finds a feature "not working," check migration state directly before assuming it's a code bug — this has now happened twice (0004 crashed the account page, 0005 broke Ask AI silently) and both times the code was fine. `src/lib/db/resilient.ts` exists specifically so a missing migration degrades a feature instead of killing it, going forward.
- **Standing rule this surfaced:** API routes must NOT be redirected by `src/proxy.ts`. A redirect hands a `fetch()` caller a 307 to an HTML page, which fails to parse as JSON and surfaces as a generic error instead of the real one. `proxy.ts` now exempts `/api/`; every route under it verifies the session itself and returns a JSON status code.
- **Model choice (user decision, Aug 15, 2026): `gemini-3.7-flash` on the Gemini free tier**, via `@google/genai` and `GEMINI_API_KEY`. Explicitly a "no billing yet" cost decision — the user plans to move to Sonnet once it matters. The route is written provider-agnostically and `route.ts` carries a PROVIDER comment naming exactly what to change to switch back (`@anthropic-ai/sdk` is still installed). Notes: **`gemini-2.5-flash` is already retired for new API keys** — don't assume a model string still works, list models and test. Pinned deliberately rather than using the `gemini-flash-latest` alias, since a silent model swap under an app advising minors should be a commit, not a surprise.
- **⚠️ AI reliability fix (Aug 16, 2026) — the free tier genuinely broke in production, not just in theory.** Reproduced both real failure modes against the live key: 503 "high demand" and 429 quota-exceeded. Fixed with `MODEL_CHAIN` in `lib/ai/guard.ts` — falls `gemini-3.7-flash` → `gemini-3.5-flash` → `gemini-3.1-flash-lite`; 3.5 reliably answers within seconds when 3.7 is saturated on the same key, so falling across models rescues far more requests than retrying one model harder. Also dropped `thinkingConfig` to `0` (was adaptive) for chat/interview — quota is the real constraint and thinking tokens cost against every message's budget, with no measurable quality loss in testing — and trimmed history 20→10 turns. 429 vs 503 now give different user-facing text (**"come back tomorrow"** vs **"try again in a few seconds"**) since they mean different things.
- **⚠️ Real scroll bug (Aug 16, 2026): the site would randomly stop responding to the mouse wheel while the scrollbar kept working.** Root cause: Lenis (smooth-scroll) caches the page's scrollable height at mount and clamps wheel scrolling to it — anything that grows content *after* mount ("Show all 24," an expanding roadmap item, a streaming chat reply) leaves that cached limit stale. The scrollbar bypasses Lenis so it kept working, which was the diagnostic tell. Fixed with a debounced `ResizeObserver` on `document.documentElement` in `SmoothScroll.tsx` — recalculates on any height change, so no expandable component has to remember to call `.resize()` itself.
- **⚠️ Safety rule found by actually testing the model, not by reasoning:** Gemini volunteered reassurance that financial-aid forms are *not* shared with immigration enforcement (citing FERPA), and confidently listed which states offer aid to undocumented students. Both are exactly what this app must never do — a false reassurance to a mixed-status family is the worst failure mode available to it. `system-prompt.ts` now carries an explicit "never reassure anyone about immigration enforcement risk" block (labelled the most important line in the prompt) plus a "don't state state-level policy as settled fact" rule; both verified to hold after the change. **Re-run that check after any prompt or model change** — the test lives in the Aug 15 Decisions Log entry.
- **Profile expansion (V2 step 2 — built Aug 15, 2026):** migration `0004_profile_details.sql` adds GPA (+scale), SAT/ACT, course rigor, target colleges, first-gen, home language, and an optional immigration-status field to `profiles` — **all nullable**. Editor is `src/components/account/ProfileDetails.tsx` on `/account`. Two rules it exists to honour: "N/A is okay" is stated on screen rather than implied, and the status field is last, visually separated, and explains why it's asked + that it's never shared *before* asking. Every field feeds `buildContextBlock()` so the chat actually uses them.
- **Activities AI interview (V2 step 3 — the differentiator, built Aug 15, 2026):** `api/activities/interview` + `api/activities/extract`, prompt in `src/lib/ai/interview-prompt.ts`, UI in `ActivityInterview.tsx`, migration `0005_chat_kind.sql`. **Separate prompt from Ask AI on purpose** — interviewing ≠ answering. Asks one question at a time, refuses to accept "I don't do anything", and treats caregiving/translating/family-business work as real activities. **Hard rule in the prompt: never inflate.** "Family Caregiving", not "Founded a childcare initiative" — the student has to defend the entry, not us. Drafts are always reviewed before saving; the model never writes to the activities list unsupervised.
- **Shared AI plumbing:** `src/lib/ai/guard.ts` holds signed-in → key → daily-cap for all three AI routes (don't hand-copy a fourth) plus `withRetry`. The retry exists because the **Gemini free tier genuinely 429s/503s about half the time** on single-shot calls — it backs off on transient statuses only, so a retired-model 404 still fails fast.
- **Gap analysis (V2 step 4, built Aug 15, 2026):** `WhereYouAre` on `/roadmap`, `lib/roadmap/gaps.ts`, `data/roadmap-timing.ts`. Zero new content — logic over the existing 51 items. **Never score the student** — no percentage, no progress bar, no "behind"; show what's still *open*. `roadmap-timing.ts` marks items evergreen vs windowed so a late joiner isn't shown five years of unchecked boxes; windowed items are filtered out and there is deliberately no "missed" bucket. Catch-up is walked **newest-grade-first** — sorted the other way the defaults were all grade-6 habits, which was useless.
- **Major pathways (V2 step 5 — PART A only, built Aug 15, 2026):** `majors.ts` now carries grade-phased `phases`, `askCounselor` (questions, never answers — the answers are school-specific and we'd be guessing), and `verify` (explicitly naming what we don't know for them). `MajorLens` shows only the phase matching the student's grade; `phaseForGrade` returns null rather than a nearest match, so a 7th grader never sees 9th-grade advice relabelled as theirs. **PART B — started Aug 16, 2026, 4 of 8 families done.** `src/data/major-opportunities.ts` + `Opportunities.tsx`. Every entry was verified by real web research on the date stamped in the file — **nothing written from memory, and nothing should be.** Done: engineering-cs, health-medicine, humanities, natural-sciences. Not done and explicitly listed in `UNRESEARCHED_FAMILIES`: arts-design, business, social-sciences, education — the UI says "not yet researched" for those rather than rendering an empty list, because an empty list reads as "there's nothing out there for you", which is false. **Rules are in the file header: only free/paid programs (never pay-to-attend — this audience can't spend $6k on a summer), cost shown first on every card, deadlines always paired with "confirm on the site", and re-verify when `verifiedOn` is over a year old.**
- **Tools page (`/tools`, **sign-in required** — V2 steps 6+7, built Aug 15, 2026):** `FeeWaiverChecker` (**never says "you don't qualify"** — thresholds change annually and a false negative costs families real money; it shows indicators + exactly who to ask, with no dollar figures) and `EssayBrainstorm` (**writes nothing, ever** — an AI-drafted essay is an integrity problem for the student and would blacklist the app with counselors, who are the #1 distribution channel; notes stay in localStorage since this is unedited thinking about family hardship). **User call Aug 15, 2026: these are support tools, not the front door** — the roadmap is the product, so `/tools` is gated and the tools are surfaced *from inside* the relevant roadmap items via `src/data/item-tools.ts` (fee waivers at the money items, essay exercises in 11–12) rather than relying on the nav. Only `/`, `/guide`+ and the auth pages stay public.
- **Freshness stamps (V2 step 8, built Aug 15, 2026):** `data/freshness.ts` + `FreshnessStamp` on every guide article. Renders in **both** states — verified articles show the date + what was checked + a watch-list; unverified ones say "not recently re-checked" rather than looking identical. **Never bump a date without genuinely re-verifying against a primary source** — a false stamp is worse than none.
- **Parent mode + Spanish (V2 step 10, built Aug 15, 2026):** migration `0006_language_preference.sql`. `account_type` finally has a UI (`LanguageAndRole` on `/account`) — it had existed unused since 0001, so every account was silently a student. Parent copy states the July 21 decision visibly: parent accounts are standalone, **not** a dashboard of their child. Spanish covers **UI chrome only** — `lib/i18n/strings.ts`. **The roadmap/guide content is deliberately NOT machine-translated**: a translation that shifts "may qualify" into "qualifies", or softens an enforcement-risk hedge, would harm exactly the families most exposed. Instead `ExplainInSpanish` on each article hands the reader to the AI, which explains it in Spanish and takes follow-ups, labelled as *an explanation, not an official translation*. Chat honours the preference and keeps FAFSA/Common App in English with a gloss. **Re-test the enforcement-reassurance guardrail in BOTH languages after any prompt change** — English-verified is not Spanish-verified.
- **Counselor share link (V2 step 9, built Aug 15, 2026):** migration `0007_share_links.sql`, `ShareLink` on `/account`, public `/s/[token]`. Student-initiated and student-revocable only — never parent-initiated, per the July 21 decision; this is a sharing tool, not a monitoring one. **This is the ONLY place data leaves an account.** `get_shared_progress` (SECURITY DEFINER) returns grade, major, completed item ids, activities — and **never** immigration status, GPA, test scores, or any chat transcript. Its return list IS the security boundary, written field by field on purpose; **adding a field to it is a privacy decision, not a feature decision.** 32-byte CSPRNG token, revocable, hard 180-day expiry. The UI shows "they will see / they will not see" before the student creates a link.
- **Not yet built (accurate as of Aug 16, 2026, later session):** V2 step 5 Part B is 4-of-8 families done (see the Major pathways entry above), the rest needs real research and must not be improvised; the net-price multi-school compare from Section 16B; and **Section 16K steps 2–8** — tools becoming individual pages, a separate settings/stats page **plus the new per-course-list schema**, the structural "what's offered/expected" comparison data, the OpenRouter + Claude Sonnet provider layer, and the flagship "Profile Analysis" tool (whose dream-college comparison piece is still blocked on the ML-model decision). **§16K step 1 — `/major` — is DONE**, see its own entry above and Section 16L. Steps 6–10 (essay brainstorm, fee-waiver checker, freshness stamps, counselor share link, parent mode) are done — see their own entries above.
- **⚠️ Read `master-spec-doc.md` Section 16K before building anything AI-related or major/tools-related.** Detailed plan captured Aug 16, 2026 at the user's explicit request ("understand everything first, don't build yet"). Settings/stats page needs **real new schema** — a per-course list the student fills in (not the coarse `course_rigor` bucket). **OpenRouter is confirmed** for resume-text generation specifically, rotating across free models, same cost posture as the Gemini decision. **The ML-model question that used to be open here is now fully RESOLVED (Aug 16, 2026) — see §16N and the brother's-project build plan in §16O, and don't re-litigate it.** The model stays the brother's side project and never ships into PathFinder; the dream-college comparison uses NCES/Common Data Set aggregate ranges instead.
- **The brother's ML project has a complete, research-verified build plan — `master-spec-doc.md` §16O (Aug 17, 2026).** Covers the actual dataset situation (verified: no individual-level U.S. undergrad admissions dataset exists publicly, confirmed against the College Scorecard API and Opportunity Insights' Mobility Report Cards), the recommended two-dataset structure (Kaggle "Graduate Admission 2" for modeling, Opportunity Insights data as sourced evidence for the limitations section), the methodology that makes it resume-strong (multiple models, SHAP feature importance, calibration curves), and how to title and present it honestly. Read it before saying anything about this project — the dataset claims were verified live, not recalled from training data, and shouldn't be re-guessed.
- **Platform note:** user is moving primary development to a MacBook. No project migration needed — clone the repo fresh, `npm install` in `app/`. Nothing machine-specific is checked in.
- **Build order (see `master-spec-doc.md` Section 16, "Next Steps Sequence"):** (1) fix/finish app content — done, (2) fix the generic-AI-look design — done but unconfirmed as final, (3) improve content further — **in progress** (roadmap middle-school depth pass done, guide articles not yet touched), (4) full design pass incl. mobile/responsive — **partially done** (real mobile fixes landed, not yet a full documented audit), (5) more features/polish incl. shrinking the AI chat UI — not started, (6) accounts/database/backend — **substantially built and now live** (see Accounts/auth above; chatbot backend specifically still not built), (7) more complex features. The user explicitly requested step 6 (Aug 15 seventh design pass) so this was not jumping ahead uninvited — but note the sequence is now non-linear (6 landed ahead of finishing 3–5), so don't assume the remaining steps are untouched without checking.

## 🚨 READ FIRST — motion levels, and the bug that wasted several sessions
**The user's machine has macOS "Reduce Motion" enabled.** Every animated surface used to gate on `prefers-reduced-motion` directly, so the 3D hero, physics pit, all page backdrops, the wire cage and every scroll reveal were correctly and silently switched **off** for them. They spent multiple rounds reporting "I don't see any 3D, nothing moves, it's static" — and they were right. Measured proof: same page, `canvasCount` went 0 → 4 after the fix.

**Never gate animation on `matchMedia("(prefers-reduced-motion: reduce)")` directly again.** Use `src/lib/motion.ts`:
- `shouldRender3D()` — true at `full` *and* `calm`; only `still` removes geometry.
- `shouldAnimateAggressively()` — `full` only. Gate bounce/overshoot, scroll-scrub, parallax and Lenis behind this.
- OS reduced-motion maps to **`calm`, never `still`** — reduce motion means calmer motion, not a dead page.
- An explicit user choice (localStorage `pf-motion`) always beats the OS. `MotionToggle` renders the control plus a first-run notice explaining the situation when the OS is suppressing things.

## ⚠️ Typography changed (Aug 14, 2026) — the serif is gone
**Display face is now Figtree (bold geometric sans), not Instrument Serif.** The serif was the single biggest reason the site read as a generic template beside the user's references (intrepidautomation.com, zypsy.com, rejouice.com) — all of which run a bold geometric sans. Use the `.display` / `.display-md` classes (weight 800/700, tight negative tracking), **not** `font-display` + `font-normal`. Instrument Serif is retained only as `--font-serif` for rare accents.

Also added and load-bearing:
- **Light-section inversion** (`bg-bone` `#eef0ed`, dark type inside). Intrepid alternates dark → light → dark, and this does more to break up a long page than any single animation. Use it; don't let the page become one black slab again.
- **`IntroLoader`** — colour-wash + counter + panel-wipe opening, once per tab via sessionStorage, with a hard 4.5s failsafe that always restores `body` overflow. It renders *over* a complete page, never as a gate in front of an empty one.
- **`WireCage`** — morphing hyperboloid built from straight lines (twist angle animates); `ClosingWire` sets huge type inside it.
- **`Marquee`** is full-bleed (`left-1/2 w-screen -translate-x-1/2`) with Zypsy-style boxed cards.

## ⚠️ Palette update (Aug 14, 2026) — colour is now allowed, as light
The strict near-monochrome rule below was **relaxed by the user**, who rejected the monochrome result repeatedly as "plain / boring / just black white and stars." Colour now enters as *light over true black*, never as flat pigment: `.aurora` (multi-stop drifting gradient field), `.glow-signal` / `.glow-ember` (emissive type — **single words only, never paragraphs**), `.edge-glow` (gradient-masked panel borders), `.scanlines`. True black grounds, Instrument Serif, hairline geometry and the editorial structure all still stand. The one deliberate exception is `.paper` (cream) in `ResumePaper`, justified contextually because an application is physically a document — it is not licence for a cream site palette. Also added: `ResumePaper`, `physics/GradePit` + `GradePitSection` (Rapier physics, canvas-texture numbers — do not swap to troika/Text3D, they fetch a font at runtime), `Marquee`.

## Design direction (rewritten Aug 14, 2026 — the old restrictive version was retired at the user's explicit request)
The user's direction is now **maximalist and premium**: heavy 3D, real physics, bold per-page colour, kinetic type. The earlier "stay near-monochrome, ask before major visual work" rules were blocking that and the user asked for them gone. What replaced them:

1. **Every page commits to ONE bold accent**, set via `data-accent` on `PageFrame` (`teal` home · `lime` roadmap · `coral` guide · `violet` ask-ai). Taken from the Intrepid reference, which uses a single coral against monochrome. Use `.glow-accent`, `.aurora-accent`, `.text-accent` — never hardcode a hex.
2. **Each page gets its own 3D geometry, not the same scene recoloured** — `Backdrop variant="grid" | "sheets" | "swarm"`. Add a new variant for a new page rather than reusing one.
3. **Text arrives, it doesn't fade** — `KineticText` (GSAP `back.out` overshoot) for headings; `SplitReveal` for calmer body-adjacent lines.
4. Reference sites the user supplied and liked: zypsy.com (boxed marquee cards, tight/small), intrepidautomation.com (corner brackets, mono index labels, one bold accent), studio-size.com, edpnc.com, collectiveoffice.com (fonts/design). Match these, not generic AI-landing-page conventions.
5. **Still true, and non-negotiable:** all WebGL is progressive enhancement behind a capability + reduced-motion gate with a real fallback; and **never author reveal-dependent content with inline `opacity: 0`** — hide at runtime, add a failsafe timeout, so failure leaves content visible rather than blank.
6. **⚠️ JSX WHITESPACE BUG — a real, user-visible class of defect in this codebase (Aug 16, 2026).** Writing `{expr} word` where the following text **wraps onto another content line** renders as `valueword` — the compiler strips the leading space of a text run whose first line is followed by more content lines. Found live as "Social**Sciencesyet**" on `/major`; three more were sitting in `WhereYouAre`, `PathwayTimeline` and `OnboardingFlow` ("completed item**sover** from this browser"). **Fix and prevention: use an explicit `{" "}` after any expression followed by wrapping prose.** It is safe when the next line is just a closing tag (`</p>`), which is why most instances are fine. A detector script is at `scratchpad/findws.mjs`; re-run the idea of it after writing prose with interpolation.
7. **First Radix usage in the project (Aug 16, 2026): `@radix-ui/react-tabs`, `@radix-ui/react-tooltip`.** Picked because they're headless — no visual opinion, so they slot into the existing look instead of fighting it. Used on `/major` for the family switcher and stage selector (real ARIA tabs, roving-tabindex arrow-key nav, replacing hand-rolled `role="radiogroup"`/`aria-pressed` code) and a new `src/components/ui/Tooltip.tsx`. **If you add a Tooltip trigger, give it `tabIndex={0}`** — a bare `<span>` isn't focusable, so Radix's keyboard-accessible reveal path (which uses `onFocus`) has nothing to attach to. Caught this exact bug on the first use.
8. **⚠️ Local-dev auth bypass in `src/proxy.ts` (Aug 16, 2026).** Gated routes render without a session when `process.env.NODE_ENV === "development"` — safe because that's automatic under `next dev` and never true on any Vercel deployment (Production or Preview both run a production build), so there's no env var to leak. Exists because Google sign-in on localhost bounces to the production Site URL (Supabase's allowed redirect list / the Google OAuth client are configured for production only — a dashboard fix, not a code one). Client components still see no user, so this shows the signed-out/no-profile state, not a personalized one.
9. **⚠️ NEW, Aug 16, 2026 — never use `<AnimatePresence mode="wait">` for content swaps.** `mode="wait"` keeps the OUTGOING child mounted until its exit animation finishes, and exit animations run on rAF. Wherever rAF is frozen (a backgrounded tab; the preview pane), the exit never completes and the incoming child never mounts — so the page keeps showing the **previous** item's content under the **new** item's heading. Caught on `/major`: it rendered Engineering / CS courses beneath the "Arts & Design" title. That's not a missing animation, it's confidently mislabelled content, which is the one thing this app must never do. Use a CSS keyframe (`.swap-in`) plus a React `key` instead. **And note the related trap:** Chrome throttles CSS animations in hidden documents too, so a swap keyframe must not start at `opacity: 0` — `.swap-in` starts at `0.55`, which stays above WCAG AA on both the black and bone grounds if it freezes on frame one.

## Content rules
- Never invent statistics, organization names, or policy specifics. If unsure, flag it for verification rather than guessing.
- All roadmap/guide content must be genuinely researched and specific — generic "study hard and get good grades" filler is a regression, not a feature.
- Content stays general/major-agnostic for V1. Major-specific personalization is V2/V3 — see `master-spec-doc.md` Section 3B. Don't build it early.

## Do NOT
- Add features outside V1 scope (see `master-spec-doc.md` Section 2) without the user explicitly approving the scope change first.
- Regenerate or "improve" the roadmap/parent-guide content without being asked — it's already been through multiple real revision passes.
- Make irreversible or costly decisions (recreating the Supabase project, force-pushing, deleting data) without explicit confirmation.

## After material changes
Update the Decisions Log and Current Build Status sections in `master-spec-doc.md` — this is a standing user instruction, not optional.

## Reference files in this repo
- `master-spec-doc.md` — full mission, scope, decisions log, build status (the deep-context doc)
- `app/` — the real Next.js app (source of truth going forward)
- `content/roadmap-content-v4.md` — human-readable source of truth for roadmap content (parses into `app/src/data/roadmap.json`)
- `pathfinder-app.jsx` — historical prototype, superseded by `app/`. Still useful as a record of the original UI interaction patterns, but no longer the source of truth for content or code.
